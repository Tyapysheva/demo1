package com.example.demo.entity;

public interface Manager extends Worker {

    /**
     * Method returning manager's department name
     *
     * @return nameOfDepartment Manager's department name
     * */
    String nameOfDepartment();
}
